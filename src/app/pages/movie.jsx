// @ts-check
// TODO: spec 13 — full-bleed trailer page (autoplay muted, hero metadata, CTAs).

const Movie = () => (
  <section className="mx-auto max-w-3xl px-6 py-24 text-center">
    <h1 className="display text-ink mb-4 text-3xl">Trailer page</h1>
    <p className="text-ink-muted text-sm">Landing in spec 13.</p>
  </section>
);

export default Movie;
